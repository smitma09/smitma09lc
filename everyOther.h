#ifndef everyOther_h
#define everyOther_h

#include <stdio.h>
#include <string.h>

/* return_type name(what_is_passed); */

void everyOther(char* arr);

#endif

/* #ifndef and endif prevents circular reference (similar to infinite loops) */
